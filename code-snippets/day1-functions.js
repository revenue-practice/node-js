const sum = (a, b) => a + b;
const isEven = (x) => x % 2 == 0;
const clamp = (n, min, max) => {
    if(n <= min) return min;
    else if(n > max) return max;
    return n;
};
const toTitleCase = (str) => {

};